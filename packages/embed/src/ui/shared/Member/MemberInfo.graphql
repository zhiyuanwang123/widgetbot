# query MemberInfo($server: ID!, $member: ID!) {
#   server(id: $server) {
#     member(id: $member) {
#       name
#       id
#     }
#   }
# }

# TODO: Fix query
query MemberInfo {
  stats {
    onlineGuests
  }
}
