{
  "name": "widgetbot",
  "private": true,
  "workspaces": [
    "packages/*",
    "src/*",
    "scripts"
  ],
  "scripts": {
    "pull": "git stash && git pull && yarn && yarn build",
    "postinstall": "git apply --ignore-whitespace patches/**.patch || true",
    "precommit": "pretty-quick --staged"
  },
  "dependencies": {
    "react": "^16.8.2",
    "react-dom": "^16.8.2"
  },
  "resolutions": {
    "**/@types/react": "16.8.3",
    "**/apollo-link": "1.2.8",
    "**/@types/graphql": "14.0.6"
  },
  "devDependencies": {
    "lerna": "^3.13.0"
  }
}
