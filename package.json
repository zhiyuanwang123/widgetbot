{
  "name": "widgetbot",
  "private": true,
  "workspaces": [
    "packages/*",
    "src/*"
  ],
  "scripts": {
    "pull": "git stash && git pull && yarn && yarn build",
    "precommit": "pretty-quick --staged"
  },
  "dependencies": {
    "react": "^16.8.1",
    "react-dom": "^16.8.1"
  },
  "devDependencies": {
    "lerna": "^3.11.1"
  }
}
